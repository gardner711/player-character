version: '3.8'

services:
  webservice:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8765:8765"
    environment:
      - GIN_MODE=test
      - LOG_LEVEL=debug
    restart: no
    networks:
      - webservice-network

networks:
  webservice-network:
    driver: bridge
